import React, { useState, useEffect, useMemo } from 'react';import { Link, useNavigate, useSearchParams } from 'react-router-dom';import {  Search,  MapPin,  Leaf,  Store,  Bookmark,  Star,  X,  ShieldCheck,  ShoppingBag,} from 'lucide-react';import { getProducts, getCategories } from '@/api/catalog';import { formatPrice } from '@/utils/format';import { PATHS } from '@/routes/paths';import useDocumentTitle from '@/hooks/useDocumentTitle';import styles from './Products.module.css';export function Products() {  useDocumentTitle('Fresh Harvest & Farm Goods ΓÇö MarketLink');  const navigate = useNavigate();  const [searchParams, setSearchParams] = useSearchParams();  const [rawProducts, setRawProducts] = useState([]);  const [categories, setCategories] = useState(['All Harvest']);  const [loading, setLoading] = useState(true);  const [searchQuery, setSearchQuery] = useState(searchParams.get('q') || '');  const [selectedCategory, setSelectedCategory] = useState(    searchParams.get('category') || 'All Harvest'  );  const [sortBy, setSortBy] = useState('featured');  const [savedItems, setSavedItems] = useState({});  useEffect(() => {    let active = true;    setLoading(true);    Promise.all([      getProducts().catch(() => []),      getCategories().catch(() => []),    ])      .then(([prods, cats]) => {        if (!active) return;        const pList = Array.isArray(prods) ? prods : prods?.items || prods?.data || [];        setRawProducts(pList);        const cList = Array.isArray(cats) ? cats : cats?.items || cats?.data || [];        const catNames = ['All Harvest', ...cList.map((c) => (typeof c === 'string' ? c : c.name || c.id))];        setCategories(catNames);      })      .finally(() => {        if (active) setLoading(false);      });    return () => {      active = false;    };  }, []);  const normalizedProducts = useMemo(() => {    return rawProducts.map((p) => {      const id = p.id || p._id;      const farmName = p.farmer?.stallName || p.farmer?.name || 'Local Farm';      return {        id,        name: p.name,        category: p.category || 'Vegetables & Greens',        price: formatPrice(p.priceCents || p.price),        unit: `/ ${p.unit || 'each'}`,        numericPrice: (p.priceCents || 0) / 100,        farmer: farmName,        farmName,        farmerId: p.farmer?.id || p.farmerId,        location: p.farmer?.location || 'Local Region',        image: p.image || '/images/product-tomatoes.jpg',        badge: p.availability === 'in_stock' ? 'In Season' : 'Fresh',        badgeType: 'in-season',        inStock: p.availability !== 'out',        stockText: p.quantityAvailable ? `${p.quantityAvailable} available` : 'Freshly harvested',        description: p.description || 'Grown with care by local producers and packed fresh for your Saturday pickup.',        rating: p.rating || 4.9,        reviews: p.reviewCount || 28,      };    });  }, [rawProducts]);  const toggleSave = (id, e) => {    if (e) e.stopPropagation();    setSavedItems((prev) => ({ ...prev, [id]: !prev[id] }));  };  const filteredProducts = useMemo(() => {    let result = normalizedProducts.filter((p) => {      if (searchQuery.trim()) {        const q = searchQuery.toLowerCase();        const matchesName = p.name?.toLowerCase().includes(q);        const matchesFarm = p.farmName?.toLowerCase().includes(q);        const matchesDesc = p.description?.toLowerCase().includes(q);        if (!matchesName && !matchesFarm && !matchesDesc) return false;      }      if (selectedCategory !== 'All Harvest' && p.category !== selectedCategory) {        return false;      }      return true;    });    if (sortBy === 'price-low') {      result.sort((a, b) => a.numericPrice - b.numericPrice);    } else if (sortBy === 'price-high') {      result.sort((a, b) => b.numericPrice - a.numericPrice);    } else if (sortBy === 'rating') {      result.sort((a, b) => b.rating - a.rating);    }    return result;  }, [normalizedProducts, searchQuery, selectedCategory, sortBy]);  return (    <div className={styles.page}>      {/* ΓöÇΓöÇΓöÇ 1. HERO SEARCH BANNER ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ */}      <section className={styles.heroSection}>        <div className="container">          <div className={styles.heroContent}>            <span className={styles.eyebrow}>              <Leaf size={14} className={styles.eyebrowIcon} />              HARVEST DIRECTORY            </span>            <h1 className={styles.heroTitle}>Explore Fresh Harvest & Artisan Goods</h1>            <p className={styles.heroSubtitle}>              Reserve seasonal fruits, heritage vegetables, fresh bread, and farm goods online. Pick up              and pay in person at your Saturday market stall.            </p>            {/* Filter Bar */}            <div className={styles.searchBarWrapper}>              <div className={styles.searchInputGroup}>                <Search size={18} className={styles.searchIcon} />                <input                  type="text"                  placeholder="Search tomatoes, sourdough, peaches, honey..."                  value={searchQuery}                  onChange={(e) => setSearchQuery(e.target.value)}                  className={styles.searchInput}                />                {searchQuery && (                  <button                    type="button"                    onClick={() => setSearchQuery('')}                    className={styles.clearSearchBtn}                    aria-label="Clear search"                  >                    <X size={15} />                  </button>                )}              </div>            </div>          </div>        </div>      </section>      {/* ΓöÇΓöÇΓöÇ 2. CATEGORY FILTER STRIP ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ */}      <div className={styles.categoryBar}>        <div className="container">          <div className={styles.categoryScroll} role="tablist">            {categories.map((cat) => (              <button                key={cat}                type="button"                onClick={() => setSelectedCategory(cat)}                className={`${styles.categoryChip} ${                  selectedCategory === cat ? styles.categoryChipActive : ''                }`}              >                {cat}              </button>            ))}          </div>        </div>      </div>      {/* ΓöÇΓöÇΓöÇ 3. SUB-FILTER & RESULTS SUMMARY ROW ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ */}      <div className={styles.filterMetaBar}>        <div className="container">          <div className={styles.filterMetaInner}>            <div className={styles.filterLeftToggles}>              <span className={styles.resultsCount}>                Showing <strong>{filteredProducts.length}</strong> items available for pre-order              </span>            </div>            {/* Sort Dropdown */}            <div className={styles.sortWrapper}>              <span className={styles.sortLabel}>Sort by:</span>              <select                value={sortBy}                onChange={(e) => setSortBy(e.target.value)}                className={styles.sortSelect}                aria-label="Sort products"              >                <option value="featured">Featured First</option>                <option value="price-low">Price: Low to High</option>                <option value="price-high">Price: High to Low</option>                <option value="rating">Highest Rated</option>              </select>            </div>          </div>        </div>      </div>      {/* ΓöÇΓöÇΓöÇ 4. PRODUCTS GRID ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ */}      <main className={styles.productsSection}>        <div className="container">          <div className={styles.productsGrid}>            {loading ? (              <div style={{ gridColumn: '1 / -1', padding: '60px 0', textAlign: 'center', color: '#6e655c' }}>                Loading fresh harvest...              </div>            ) : filteredProducts.length > 0 ? (              filteredProducts.map((p) => {                const isSaved = savedItems[p.id];                return (                  <article                    key={p.id}                    className={styles.productCard}                    onClick={() => navigate(`/products/${p.id}`)}                  >                    <div className={styles.imageWrap}>                      <img src={p.image} alt={p.name} className={styles.productImg} />                      <span className={styles.badgeSeason}>{p.badge}</span>                      <button                        type="button"                        onClick={(e) => toggleSave(p.id, e)}                        className={`${styles.bookmarkBtn} ${isSaved ? styles.bookmarkBtnActive : ''}`}                        aria-label="Save product"                      >                        <Bookmark size={15} fill={isSaved ? '#541722' : 'none'} />                      </button>                    </div>                    <div className={styles.cardContent}>                      <div className={styles.vendorRow}>                        <span className={styles.vendorName}>{p.farmName}</span>                        <div className={styles.ratingBadge}>                          <Star size={11} fill="#D4850A" color="#D4850A" />                          <span>{p.rating}</span>                        </div>                      </div>                      <h2 className={styles.productTitle}>                        <Link to={`/products/${p.id}`} className={styles.titleLink}>                          {p.name}                        </Link>                      </h2>                      <p className={styles.description}>{p.description}</p>                      <div className={styles.cardBottomRow}>                        <div className={styles.priceGroup}>                          <span className={styles.price}>{p.price}</span>                          <span className={styles.unit}>{p.unit}</span>                        </div>                        <span className={styles.stockNote}>{p.stockText}</span>                      </div>                      <div className={styles.cardActions}>                        <Link to={`/products/${p.id}`} className={styles.reserveBtn}>                          <ShoppingBag size={14} />                          <span>Pre-Order Details</span>                        </Link>                      </div>                    </div>                  </article>                );              })            ) : (              <div style={{ gridColumn: '1 / -1', padding: '60px 0', textAlign: 'center', color: '#6e655c' }}>                <h3>No harvest items found</h3>                <p>Try searching for a different crop or resetting your category filter.</p>              </div>            )}          </div>        </div>      </main>    </div>  );}export default Products;