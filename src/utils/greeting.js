export function getGreeting(name) {  const hour = new Date().getHours();  let period;  if (hour < 12) {    period = 'morning';  } else if (hour < 18) {    period = 'afternoon';  } else {    period = 'evening';  }  return `Good ${period}, ${name}`;}