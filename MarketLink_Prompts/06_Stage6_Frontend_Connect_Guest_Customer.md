# MarketLink Stage 6 of 9: Connect the Front End (Guest, Auth and Customer App)You are a **senior front-end engineer**. The backend (Stages 1 to 5) is complete and verified. **Stage 6 replaces all placeholder data in the guest pages, the auth flow and the entire Customer app with the real API**, adds the real map, and keeps the UI as minimal, fast and app-like as it is now. Farmer and Admin front ends are Stage 7; do not touch them except where shared code requires."Must" is not optional. Read this whole prompt first.## Project context (read this first: you are starting a fresh session with no memory)**MarketLink** is a farmers-market **pre-order for pickup** web app (React 18 + Vite front end, Node/Express + MongoDB backend in `backend/`, already built and verified). Roles: **Customer** (`customer`), **Farmer** (`farmer`), **Admin** (`admin`). No payment gateway, no delivery: Customers pay in person at pickup. **On screen always write "Customer" and "Farmer"**, never "buyer" or "vendor" (only folders and route prefixes `/buyer`, `/vendor`, `/admin` use those words; map API roles `customer → /buyer`, `farmer → /vendor`, `admin → /admin`). Order statuses shown to users are exactly: **Placed, Accepted, Ready for pickup, Completed, Cancelled** (API values `placed, accepted, ready, completed, cancelled, declined`; `declined` displays as Cancelled with a reason).**Front-end rules that still apply (from the design system in `docs/DESIGN_SYSTEM.md`):**- React 18, plain JSX, `react-router-dom`, `lucide-react`. **CSS Modules** next to each component plus the three global CSS files (`tokens.css`, `base.css`, `utils.css`). **No Tailwind, SCSS, Bootstrap, CSS-in-JS, UI kits.** New packages only with a one-line justification.- Modules use `var(--token)` only (literals allowed: `0`, `1px`, `100%`, `auto`, breakpoints 480 / 768 / 1024 / 1280px). Mobile-first. No `!important` (except reduced-motion), no id selectors, max two nesting levels.- **Light theme, mostly white.** Beet = primary button/active nav/links, carrot = warnings, herb = success, wood = hairlines. No gradients, no card shadows, no decorative colour. Idiqlat headings (weight 400, 18px+), Inter for the rest. 44px minimum touch targets. **One filled beet button per screen, per sheet, per step.**- **Super minimal, clean, elegant, uncongested.** Follow the density budgets and minimal-UI rules from the repair pass. Customer app is **sheet-first**: tabs are pages, things you open are sheets (background-location pattern, replace-not-stack, thumb-draggable `BottomSheet`), bottom nav below 1024px, top nav from 1024px. Use `window.layoutCheck()` (dev tool in `src/dev/layoutCheck.js`): it must report **zero findings** at 320, 360, 390, 768, 1024, 1440 for every page and sheet you touch.- Code is simple and explainable by students: function components and hooks only, short top-of-file comments, no clever abstractions.**Backend contract:** `/api` base path. Success `{ data, meta }`; errors `{ error: { code, message, details:[{field,message}] } }`. IDs are string `id`. **Money is integer cents** (format with `formatPrice` in `utils/format.js`). Dates are ISO strings. Access token is a 15-minute JWT sent as `Authorization: Bearer`; the refresh token is an httpOnly cookie (`/api/auth/refresh`). The authoritative reference is `backend/docs/API.md`: read it, do not guess.## Session start checklist (do this before writing code)1. Read `backend/docs/API.md`, `backend/README.md`, `docs/DESIGN_SYSTEM.md`, `src/routes/AppRoutes.jsx`, `src/context/*`, `src/api/*` (if it exists).2. Start MongoDB, `cd backend && npm run seed && npm test` (must be green), then `npm run dev` for the API, and `npm run dev` in the front end. Confirm you can sign in with the seeded demo accounts (credentials are in `backend/README.md`).3. Write a short plan (files to create or change, in order), then build.## If the front end needs data or behaviour the API does not offerDo **not** invent placeholder data. **Add it to the backend** (route, service with index-backed native queries, validator, tests, `docs/API.md` entry) following the backend conventions in `backend/README.md` (`src/modules/<feature>/*.routes.js` + `*.service.js`, `node:test` tests, cents, `id`, cursor pagination, no COLLSCAN), run the backend suite, and list every addition in your report.---## 1. API layer (`src/api/`)Build a small, readable API layer. **No new data-fetching library unless the hand-written hooks become bug-prone (then `@tanstack/react-query` is allowed with a one-line justification).**- `src/api/client.js`: `apiFetch(path, { method, body, query, signal })`  - Base URL from `import.meta.env.VITE_API_URL` (default `/api`). In development use a **Vite proxy** (`/api` → `http://localhost:4000`) so cookies are same-origin. Document `.env.example` for the front end.  - Sends JSON, `credentials: 'include'`, `Authorization: Bearer <accessToken>` from an **in-memory** token holder (never `localStorage`).  - On `401 TOKEN_EXPIRED` (or 401 with a refresh cookie present) performs **one single-flight refresh** (`POST /auth/refresh`), retries the original request once; if refresh fails, clears auth and redirects to `/login` with a message ("Please sign in again.").  - Normalises failures into an `ApiError { status, code, message, details }`; network failure → `code: 'NETWORK'`. Supports `AbortController` (cancel on unmount and on new searches).  - `Idempotency-Key` helper for checkout (`crypto.randomUUID()` created when the Cart sheet opens and reused for retries).- One file per domain: `auth.js`, `catalog.js` (categories, markets, farmers, products, search, feed), `orders.js` (quote, checkout, list, detail, modify, cancel, reorder, reviews), `me.js` (profile, saved markets, favourites, notifications, summary), `assistant.js`, `contact.js`. Thin functions only.- Hooks in `src/hooks/`: `useQuery(key, fetcher, { enabled, keepPrevious })` (in-memory cache, in-flight de-duplication, stale-while-revalidate for 15 s, abort on unmount, `refetch`, `data/error/loading`), `useMutation(fn)` (`mutate`, `loading`, `error`, and cache invalidation by key prefix), `useDebouncedValue`, `useInfiniteList(fetchPage)` (cursor pagination with `loadMore`, `hasMore`, dedupe by `id`). Keep each hook short and commented. Handle React StrictMode double-invocation without duplicate requests.- Money and dates: `formatPrice(cents)`, `formatDate`, `formatPickup(slot, tz)`, `formatCountdown(cutoffAt)` in `utils/format.js`. **Never compute money with floats.** Pickup times display in the **market's timezone**.---## 2. Auth (`AuthContext`) becomes real- `user` shape from `/auth/me`; roles map to areas: `customer → /buyer`, `farmer → /vendor`, `admin → /admin` (one constant, used everywhere, plus a helper `homePathFor(role)`).- On app load: try `POST /auth/refresh` **silently** (no flash of the login page), show a minimal branded loading screen for at most the first 800 ms, then either restore the session or show guest pages. `ProtectedRoute` waits for this ("checking session") before redirecting.- `login(email, password)`, `logout()` (calls the API, clears cache and cart state in memory, navigates home), `registerCustomer`, `registerFarmer`, `refreshUser()`.- Blocked accounts show the API's warm message under the form (inactive Customer, suspended Farmer). A **pending Farmer** who signs in is routed to the Farmer area (Stage 7 builds it; for now redirect to `/vendor` and keep the route working).- **Remove the TEMP demo buttons and `demoUsers` from the Login page and code.** The demo credentials live in `backend/README.md` only.- Cross-tab sign-out: use a `storage` event on a non-sensitive flag key so signing out in one tab signs out the others.---## 3. Guest pages (all real)- **Login**: real form, field errors from `details`, generic credentials error, lockout (`TOO_MANY_ATTEMPTS`) message, disabled button while submitting, focus to the first error.- **Register** (Customer and Farmer flows): submit to `/auth/register/customer` and `/auth/register/farmer`; map `422 details` to fields; `409 EMAIL_TAKEN` shows "That email already has an account. Try signing in."; the Customer is signed in and routed to `/buyer` after a warm welcome moment; the Farmer sees the "Waiting for approval" success state (already designed) and is signed in.- **Forgot password** → `/auth/forgot-password` (always shows the "Check your inbox" state). **Add the `/reset-password?token=` page** (new guest route in `paths.js`/`AppRoutes.jsx`, same minimal form style): new password and confirm, submit, success state with a "Go to sign in" button, clear errors for invalid/expired tokens.- **Contact** → `/contact` (topic values `order|farmer-help|feedback|other`), success and error states, rate-limit message. The static team details come from **`src/content/siteContent.js`** (one file: team members, address, phone, email, hours, map coordinates), which the team edits; mark each field `- **Guest Home**: the price board, farmers and announcements come from `GET /public/home` (loading skeleton that matches the layout, graceful empty state if no data). Keep the scattered-produce hero unchanged (decorative, not data).- **Announcements**: a slim, dismissible `AnnouncementBar` (one at a time, newest first, dismissal remembered per announcement id in `sessionStorage`) in both the guest and Customer layouts, fed by `GET /announcements`. Minimal, white, hairline border, no colour.- **About**: content from `siteContent.js`.---## 4. Customer app (all real, all states)Keep the current design, sheet-first navigation, minimal density and animations. Replace every placeholder. For each screen implement **loading (skeleton matching final layout), empty, error (with a retry button), and success** states.- **Home feed (`/buyer`)**: `useFeed` now calls `GET /feed` (`cursor`), appends server-built sections, keeps the sentinel + skeleton row + cache/scroll restore, shows the greeting from the user's first name and the sub-line from `GET /feed/meta`, the "Ready for pickup" card from `GET /home/summary`. "See all" links use the section's `seeAll` path and query. Never show duplicate items (dedupe by id across sections in view). Re-tapping the Market tab refreshes from batch 0.- **Browse**: server-side search (debounced 250 ms), `GET /search/suggestions` dropdown, recent searches from `/search/history` (record on submit), category chips from `/categories`, filter sheet (price, category, market, day, sort, "include sold out") mapped to query params **kept in the URL**, cursor "infinite" list with skeleton tail, empty state, result count when available.- **Markets / Market sheet**: list from `/markets` with day chips and an optional **"Use my location"** button (browser geolocation on user action only, graceful denial), distance labels, `directionsUrls` buttons ("Get directions" opens Google or OpenStreetMap in a new tab), Farmers and fresh products rows from the market endpoints, "Set as my market" (`PUT /users/me/home-market/:id`) and save/unsave (`saved-markets`).- **Farmers / Farmer sheet**: list with search, sort and filters; the sheet has Stock, Reviews (with rating breakdown, cursor pagination), About (markets, operating days, pickup windows, map). Heart uses favourites.- **Product sheet**: `GET /products/:id`, related rows, reviews, next pickup slots and cut-off countdown from `farmerCutoff`, low-stock note using `quantityLeft`, add to cart limited by `quantityAvailable`.- **Cart**: cart state stays in `CartContext` and is **persisted in `localStorage` per user id** (only product ids, quantities, chosen slot per Farmer; never prices). When the Cart sheet opens or quantities change (debounced 300 ms) it calls **`POST /cart/quote`** and renders **server truth**: current prices, availability, issues ("Only 3 left", "No longer available", "Price changed": fix or remove buttons), Farmer pickup slots (chips, only open slots), cut-off warnings (carrot), totals. "Place pre-order" is disabled while any blocking issue exists or the quote is loading. Checkout calls `/orders/checkout` with the idempotency key; on success clear the cart, show the in-sheet confirmation step, invalidate orders/feed/summary caches; on `NOT_ENOUGH_STOCK` or `CUTOFF_PASSED` show a clear message and refresh the quote.- **Orders / Order sheet**: `GET /orders?tab=`, cursor list; detail with timeline, pickup card (real map, `directionsUrls`), modify (quantities, note, slot) and cancel as in-sheet steps with API errors handled (`CUTOFF_PASSED` → the actions disappear with an explanation), "Buy again" via `reorder-preview` (adds available items, tells about unavailable ones), review form (`POST /orders/:id/reviews`) after completion.- **Favourites**: hearts everywhere use `GET /favorites/ids` (loaded once after sign-in) with **optimistic** toggles and rollback on failure; lists from `/favorites`.- **Notifications**: unread dot on the "You" tab and a **Notifications sheet** opened from Profile (list, mark read, mark all read). Poll `/notifications?unread=true` (count only) every 60 s **only while the tab is visible** (`visibilitychange`), and refresh when a sheet opens.- **Assistant sheet**: `POST /assistant/message`; render text and cards (product/farmer/market cards open their sheets), suggestion chips; typing indicator text while waiting; errors handled.- **Profile and settings sheets**: real profile from `/users/me` and `PATCH`, saved markets, notification preferences (toggles save immediately with a tiny "Saved" confirmation), change password (`/users/me/password`, errors mapped), sign out here and everywhere, About/Help static content.- **Everything shown must come from the API**: prices, stock, cut-off countdowns, statuses, timelines, ratings, counts, names, markets, distances. **No hard-coded arrays, no fake delays (`setTimeout` used as loading), no random data.** (The illustration art keys come from the API `art` field.)- Consistent handling of: 401 (silent refresh, then login), 403 (warm message), 404 (sheet shows "This isn't available any more" with a close/back action), 429 (message with retry hint), 5xx and network (banner "Can't reach MarketLink. Check your connection." plus retry), and **offline** (`navigator.onLine`).---## 5. Real maps (replace `MapPlaceholder` everywhere it is used in guest and Customer areas)- Add **`leaflet`** (one-line justification: free OpenStreetMap maps, no API key) and build one shared **`MapView`** component in `components/domain/`: props `markers [{ id, lat, lng, label }]`, `selectedId`, `onSelect`, `height`, `zoom`, `interactive`. Plain `leaflet` in a `useEffect` (no `react-leaflet`), OSM tiles with the required attribution, `fitBounds` to markers, **custom `divIcon` markers styled with tokens** (beet pin, no default image assets), a popup or a tap-to-select sheet, keyboard-accessible (map container focusable, "Zoom in/out" buttons labelled), lazy-loaded (`React.lazy`) so it does not bloat the initial bundle, cleans up on unmount, respects reduced motion (`zoomAnimation`/`fadeAnimation` off).- Use it for: Markets map view (all markets, tap a marker to see the market summary), Market sheet, Farmer About tab (stall pin), Order pickup card (single marker + "Get directions"), and the **guest Contact page** (team location from `siteContent.js`).- Keep the calm look: hairline border, `--radius-lg`, tile layer unchanged, controls minimal. If tiles fail to load, show a friendly fallback and the directions link.- Update `docs/DESIGN_SYSTEM.md` (API layer, MapView, states, announcement bar) and the front-end README (`VITE_API_URL`, proxy, running both servers).---## 6. Backend additionsIf any screen needs data the API lacks, **add it to the backend** (see the rule above), with tests, docs and a mention in your report. Do not fake it in the front end.---## 7. Quality requirements (unchanged bar)- Density, spacing, chips, bottom stack, sheet dragging and animations exactly as in the repaired design; **`layoutCheck()` zero findings** at 320, 360, 390, 768, 1024, 1440 on every guest page, Customer page and sheet, **with real data loaded** (including long names and many items).- Accessibility: focus management in sheets, `aria-live` for loading and errors, form errors linked to fields, map keyboard access, reduced motion respected.- Performance: no request waterfalls that could be one call, no duplicate calls on mount, list pages virtualisation not required but DOM must stay light (feed sections use `content-visibility`), route-level code splitting for maps and rarely used sheets, Lighthouse mobile Performance 90+ and Accessibility 95+ on Home.- Only "Customer" and "Farmer" on screen; one filled beet button per screen, sheet and step.---## 8. Acceptance checklist- [ ] No guest or Customer file imports `data/placeholders.js` or defines fake data (grep proof); the TEMP buttons and `demoUsers` are gone- [ ] Sign in, silent refresh on reload, sign out (and cross-tab), expired-token retry, blocked-account messages, password reset flow all work end to end- [ ] Register works for both roles with field-level errors; Contact form stores messages- [ ] Feed is endless from the real API with restored scroll after a sheet closes; Browse filters live in the URL and paginate- [ ] Cart uses `/cart/quote` truth; checkout is idempotent; stock and cut-off errors are handled gracefully- [ ] Orders, modify, cancel, reorder, reviews, favourites (optimistic), notifications, assistant, profile and settings all use the API- [ ] Real Leaflet/OpenStreetMap maps on Markets, Market, Farmer, Order and Contact with directions links- [ ] Loading, empty, error, offline and rate-limit states exist and were tested deliberately- [ ] `layoutCheck()` clean; Lighthouse targets met; no console errors or warnings- [ ] Any backend additions have tests and docs; backend suite still green---# MarketLink: Testing Gate (paste at the END of every stage prompt)**This stage is NOT finished until everything below is proven by running it.** Do not say "should work". Show evidence, or say "not verified".## B. For front-end integration stages (6, 7, 8)1. **Run backend and front end together** and walk through **every page and every action** in the browser (phone size and desktop size). For each page record: which API calls it makes (open the Network tab), the status codes, and that the UI shows real data.2. **Placeholder proof**: run `grep -rn "placeholders" src` (and any old hard-coded arrays) and show that **no page or component imports placeholder data**. Delete `placeholders.js` in Stage 8 once nothing uses it.3. **All states work with real data**: loading (skeletons), empty, error (server down, 401 expired token with automatic refresh, 403, 404, 422 field errors shown under fields, 429), and success. Try each on purpose (stop the server, use a wrong password, sign in as a suspended Farmer).4. **Auth flows**: sign in, refresh on page reload, sign out, expired access token refreshes silently, blocked accounts show a clear message, role redirects work, wrong-role access shows the Unauthorized page.5. **No console errors or warnings**, no failed network requests except the ones you triggered on purpose.6. **Performance**: no request waterfalls that could be one call, no duplicate calls on mount (React StrictMode double calls handled), lists paginated or cursor-loaded, images/illustrations unchanged in weight. Check Lighthouse on Home.7. Re-run the earlier front-end quality checks (`layoutCheck()` if present: zero findings at 360, 390, 768, 1024, 1440).8. **Report table**: page or sheet, API calls used, states tested (loading / empty / error / success), result.## A. For backend stages (2 to 5, and any backend additions in 8)1. **Write automated tests first-class, not as an afterthought.** For every new endpoint cover: the happy path, every validation error (422), missing or invalid token (401), wrong role (403), not found (404), conflicts (409), edge cases (empty lists, limits, boundary values), and ownership rules (a Farmer can never touch another Farmer's data; a Customer never sees another Customer's orders).2. **Run the full suite from a clean database** (`npm run seed` then `npm test`). Paste the real output (pass/fail counts, duration). **All earlier stages' tests must still pass** (regression). Fix every failure. Never delete or weaken a test to make it pass.3. **Manually call every new endpoint** with `curl` (or extend `scripts/smoke.sh`) and record the status code and a trimmed response for each. Test with each seeded role.4. **Performance proof**: run `explain('executionStats')` on every new query and confirm **no `COLLSCAN`**, `totalDocsExamined` close to `nReturned`, and projections in place. Measure response times on the seeded database and confirm the budget (reads under 50 ms, feeds and search under 80 ms, writes under 100 ms, excluding bcrypt). Log and fix anything slower.5. **Concurrency and integrity proof** where relevant (stock, order numbers, unique constraints): run parallel requests in a test and show the results are correct (no overselling, no duplicate order numbers).6. **Security check on the new routes**: injection attempts, oversized input, role bypass attempts, IDOR attempts (changing an id in the URL to someone else's), and rate limits where applicable.7. **Update `docs/API.md` and `docs/DATABASE.md`** so they match reality (every route documented with an example).8. **Report table**: endpoint, method, auth, tests written, tested manually (yes/no), result. Any "no" must be explained and fixed before finishing.9. Confirm the front end still starts and no front-end file was broken.## Rules for the report at the end of every stage- Start with **"Stage N status: PASS or FAIL"** and justify it.- Include the **real command output** or screenshots of results.- List **what you did not verify** and **what you would test next**.- If any check fails, **fix it and re-run**; do not hand back a failing stage.