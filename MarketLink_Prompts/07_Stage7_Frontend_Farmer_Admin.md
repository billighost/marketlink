# MarketLink Stage 7 of 9: Build and Connect the Farmer and Admin Front EndsYou are a **senior product designer and front-end engineer**. The guest pages, auth and Customer app are connected to the real API (Stage 6). The **Farmer (`/vendor`) and Admin (`/admin`) areas are still skeletons**. **Stage 7 builds every Farmer and Admin page fully, directly on the real API (no placeholder data at any point), with the same calm, minimal, mostly-white, elegant design** as the Customer app, working perfectly on phone, tablet and desktop."Must" is not optional. Read this whole prompt first.## Project context (read this first: you are starting a fresh session with no memory)**MarketLink** is a farmers-market **pre-order for pickup** web app (React 18 + Vite front end, Node/Express + MongoDB backend in `backend/`, already built and verified). Roles: **Customer** (`customer`), **Farmer** (`farmer`), **Admin** (`admin`). No payment gateway, no delivery: Customers pay in person at pickup. **On screen always write "Customer" and "Farmer"**, never "buyer" or "vendor" (only folders and route prefixes `/buyer`, `/vendor`, `/admin` use those words; map API roles `customer → /buyer`, `farmer → /vendor`, `admin → /admin`). Order statuses shown to users are exactly: **Placed, Accepted, Ready for pickup, Completed, Cancelled** (API values `placed, accepted, ready, completed, cancelled, declined`; `declined` displays as Cancelled with a reason).**Front-end rules that still apply (from the design system in `docs/DESIGN_SYSTEM.md`):**- React 18, plain JSX, `react-router-dom`, `lucide-react`. **CSS Modules** next to each component plus the three global CSS files (`tokens.css`, `base.css`, `utils.css`). **No Tailwind, SCSS, Bootstrap, CSS-in-JS, UI kits.** New packages only with a one-line justification.- Modules use `var(--token)` only (literals allowed: `0`, `1px`, `100%`, `auto`, breakpoints 480 / 768 / 1024 / 1280px). Mobile-first. No `!important` (except reduced-motion), no id selectors, max two nesting levels.- **Light theme, mostly white.** Beet = primary button/active nav/links, carrot = warnings, herb = success, wood = hairlines. No gradients, no card shadows, no decorative colour. Idiqlat headings (weight 400, 18px+), Inter for the rest. 44px minimum touch targets. **One filled beet button per screen, per sheet, per step.**- **Super minimal, clean, elegant, uncongested.** Follow the density budgets and minimal-UI rules from the repair pass. Customer app is **sheet-first**: tabs are pages, things you open are sheets (background-location pattern, replace-not-stack, thumb-draggable `BottomSheet`), bottom nav below 1024px, top nav from 1024px. Use `window.layoutCheck()` (dev tool in `src/dev/layoutCheck.js`): it must report **zero findings** at 320, 360, 390, 768, 1024, 1440 for every page and sheet you touch.- Code is simple and explainable by students: function components and hooks only, short top-of-file comments, no clever abstractions.**Backend contract:** `/api` base path. Success `{ data, meta }`; errors `{ error: { code, message, details:[{field,message}] } }`. IDs are string `id`. **Money is integer cents** (format with `formatPrice` in `utils/format.js`). Dates are ISO strings. Access token is a 15-minute JWT sent as `Authorization: Bearer`; the refresh token is an httpOnly cookie (`/api/auth/refresh`). The authoritative reference is `backend/docs/API.md`: read it, do not guess.## Session start checklist (do this before writing code)1. Read `backend/docs/API.md`, `backend/README.md`, `docs/DESIGN_SYSTEM.md`, `src/routes/AppRoutes.jsx`, `src/context/*`, `src/api/*` (if it exists).2. Start MongoDB, `cd backend && npm run seed && npm test` (must be green), then `npm run dev` for the API, and `npm run dev` in the front end. Confirm you can sign in with the seeded demo accounts (credentials are in `backend/README.md`).3. Write a short plan (files to create or change, in order), then build.## If the front end needs data or behaviour the API does not offerDo **not** invent placeholder data. **Add it to the backend** (route, service with index-backed native queries, validator, tests, `docs/API.md` entry) following the backend conventions in `backend/README.md` (`src/modules/<feature>/*.routes.js` + `*.service.js`, `node:test` tests, cents, `id`, cursor pagination, no COLLSCAN), run the backend suite, and list every addition in your report.---## 1. Design direction- **Same design language** as the Customer app: tokens, hairlines, Idiqlat headings, Inter body, soft line-and-tint illustrations, generous whitespace, **very few elements per screen**, one filled beet button per screen/sheet/step, no gradients, no card shadows.- Farmers are often on a **phone at the stall** or a tablet, sometimes older and non-technical: text is large enough (body 15px+, inputs 16px), buttons say exactly what happens ("Mark ready for pickup", "Accept order", "Save changes"), destructive actions always confirm, and **every empty state says what to do next with one button**.- Admins mostly use **desktop**, but every admin page must still be **usable and uncluttered on tablet and phone** (tables stack into labelled rows below 768px).- **Sheet-first** like the Customer app: lists are pages; anything opened (order details, product form, review reply, confirmations, market form, person details) is a **`BottomSheet`** (tall or peek, drawer on 768px+) using the same background-location pattern, `SheetRoute`, thumb-draggable behaviour, replace-not-stack, in-sheet steps for confirmations. Keep the routes in the original route table (`/vendor/stock/new`, `/vendor/stock/:id/edit`, `/vendor/orders/:id` open as sheets over their list page, with full-page fallbacks on direct load).### Navigation- **Farmer**: `≥1024px` left `Sidebar` (6 items: Overview, Stock, Orders, Reviews, Insights, My stall, icon + label, active = beet-tint + beet text + `aria-current`); **below 1024px a bottom nav with 5 items**: Overview, Stock, Orders (badge with the count of new orders), Reviews, More (opens a peek sheet listing Insights, My stall, Account, Sign out). Slim top bar with the stall name and a small status text.- **Admin**: `≥1024px` sidebar (6 items: Overview, People, Markets, Moderation, Reports, Settings); below 1024px a slim top bar with a labelled **"Menu" button** opening a full-width drawer. Badges on People (pending Farmers) and Moderation (open flags).- Reuse `Sidebar`, `TopBar`, `BottomNav`, `BottomSheet`, `Table`, `Tabs`, `SegmentedControl`, `Chip`, `ListRow`, `Toggle`, `QuantityStepper`, `EmptyState`, `Skeleton`, `Toast`. Add new shared components only when used in 2+ places.### Live countsFarmer nav badge (new orders) and Admin badges refresh by **polling every 45 s only while the tab is visible** (`visibilitychange`), and after any relevant action. No websockets.---## 2. Farmer pages (`src/pages/vendor/`, each with its own `.module.css`)All data from `/api/farmer/*` and the Stage 2 to 4 endpoints. Loading skeletons, empty, error, and success states everywhere.### 2.1 Overview (`/vendor`)- Greeting ("Good morning, Marta"), one quiet status line ("Elm Street Market · Saturday · Order cut-off in 5 h 20 min"), from `GET /farmer/overview`.- **Pending approval**: if the Farmer is `pending`, show one calm card at the top ("Your stall is waiting for approval. You can set up your profile while you wait.") with a text link to My stall, and product creation buttons are hidden or disabled with the reason.- **New orders** list (max 5, each: order number, Customer first name, pickup time, item count, total; tapping opens the Order sheet). Quick actions: secondary "Accept" and text "Decline" only if it stays uncluttered; otherwise open the sheet.- **This week** quick numbers (three only): Orders, Pending, Revenue (cents formatted).- **Low stock** list (up to 5, tap to open the product edit sheet).- The single primary button: "Add a product" (hidden when not approved).### 2.2 Stock (`/vendor/stock`, sheets `/vendor/stock/new`, `/vendor/stock/:id/edit`)- Search field, one chip row (All, In stock, Low, Sold out, Hidden), list of stock rows (illustration or image, name, price + unit, quantity, availability `StatusDot` only for Low/Sold out/Hidden) with an inline **"Sold out" toggle** and a stepper for quick quantity changes (debounced save, optimistic with rollback and toast).- **Weekly template** section (collapsed by default): "Weekly template" opens a tall sheet listing products with an enable switch and a default quantity; footer primary "Save template"; a secondary **"Apply weekly template"** button on the Stock page with a confirm peek sheet ("This resets quantities for N products.") and a summary toast.- **Product form sheet** (add/edit): name, category (select from `/categories`), price (dollars input → cents, validated, `$4.50` style), unit (select), quantity, low-stock level, description, tags (Seasonal, Organic), illustration picker (grid of art keys) **or** image upload (`POST /farmer/uploads/image`, preview, remove, client-side checks for type and 1 MB before upload, clear errors), weekly template toggle and default quantity. Field errors from the API `details`. Footer: one primary ("Add product" / "Save changes"), a text "Delete product" in edit mode opening an in-sheet confirm step (explains hard vs soft delete result).- Pagination via cursor "Load more" or infinite scroll; bulk actions via a simple "Select" mode (hide, mark sold out) only if it stays minimal.### 2.3 Orders (`/vendor/orders`, sheet `/vendor/orders/:id`)- `Tabs` or segmented control with counts: **New, Accepted, Ready for pickup, Completed, Cancelled** (from `meta.counts`); cursor lists of order rows (number, Customer name, pickup slot, items summary, total, `StatusDot`).- **Pickup day view**: a secondary "Pick list" button opens a sheet with the packing list for a chosen date (`/farmer/orders/pick-list`): items with total quantities, then the orders per time slot. Printing is not required; make it readable.- **Order sheet**: status and timeline, items with quantities and prices, Customer first name and last initial, the note, phone only after accepting (as the API returns), pickup slot and cut-off, total ("Paid at pickup"). **Actions by state (one primary):** New → primary "Accept order", text "Decline order" (in-sheet step asking for a reason); Accepted → primary "Mark ready for pickup", text "Cancel order" (reason step); Ready → primary "Mark completed", text "Cancel order"; terminal states show no actions. API errors (`INVALID_TRANSITION`) handled: refresh and explain.### 2.4 Reviews (`/vendor/reviews`)- Rating summary (average, count, 5-bar breakdown drawn with simple CSS bars), chip filter (All, Unanswered), review list (stars, comment, Customer name as given, product/Farmer target, date, your reply if any). **Reply sheet** (textarea, char counter, primary "Post reply", edit and delete reply). "Report" text action calls the flag endpoint with a short reason step. Empty state: "No reviews yet. They appear after Customers collect orders."### 2.5 Insights (`/vendor/insights`)- Range segmented control (7 days, 30 days, 90 days). Three key numbers (Total orders, Pending orders, Revenue), plus average order value and repeat Customers as quiet secondary lines. **Orders per day** as a simple **SVG bar chart** written by hand (no chart library): accessible (`role="img"` with a text summary, data table alternative in a `details`), tokens for colour (ink-soft bars, beet for the selected day), responsive width, labelled axis with few ticks. **Best sellers** list (top 5, quantity and revenue). Skeletons matching layout; empty state when no orders.### 2.6 My stall (`/vendor/stall`)Grouped, settings-style sections (each opens as a tall sheet or expands, keep the page itself very calm): **Stall details** (name, contact person, phone, specialty, story, since), **Markets** (multi-select from `/markets`), **Operating days and pickup windows** (day chips; per day a start and end time picker using `select`s in 30-minute steps; validation), **Order cut-off** (hours before pickup, mapped to `cutoffMinutesBefore`) and **slot capacity**, **Closed dates** (`slotOverrides`: pick dates to close/unclose, list with remove), **Location** (address plus **map pin**: `MapView` with a draggable marker and latitude/longitude fields kept in sync, "Use my current location" on user action), **Photo** (upload). Saving shows a "Saved" toast. Pending Farmers can edit all of this.---## 3. Admin pages (`src/pages/admin/`)All data from `/api/admin/*`. Tables use the shared `Table` (stacks into labelled rows below 768px), cursor "Load more", search inputs debounced 250 ms, chips for status. **Every destructive or status-changing action opens a confirm step (peek sheet) and shows a toast on success.**### 3.1 Overview (`/admin`)Four key numbers only (Farmers, Customers, Markets, Orders), a quiet **"Needs attention"** list (pending Farmers, open flags, unhandled messages, each a text link to the right page with a count), and **Recent activity** (last 10 events from the audit log/registrations/orders, one line each with relative time).### 3.2 People (`/admin/people`)`Tabs`: **Farmers | Customers**. Search + status chips (Farmers: All, Pending, Active, Suspended, Rejected; Customers: All, Active, Inactive). Table columns: Name (stall name for Farmers), Email, Status (`Badge`/`StatusDot`), Joined, action button "Open". **Person sheet**: profile summary, recent orders count, and status actions with a reason step where required: Farmers → Approve (primary when pending), Reject, Suspend, Reinstate; Customers → Deactivate, Activate. Admins never see actions for admin accounts. Empty states per tab.### 3.3 Markets (`/admin/markets`)List/table with name, address, days, Farmers count, status. **Market form sheet** (add/edit): name, address, **map location** (`MapView` with a draggable marker + lat/lng fields, and an optional map link field validated against Google Maps/OpenStreetMap), weekly schedule editor (day, open, close), timezone select, facilities chips, note. Remove market: confirm step explaining Farmers are detached if `force` is needed. Primary "Add market".### 3.4 Moderation (`/admin/moderation`)`Tabs`: **Listings | Reviews** showing open flags (reason, reporter, date, a **preview of the flagged item**). Sheet with the preview and two actions: primary "Remove" (with note step) and secondary "Keep and dismiss". A search box lets the Admin find any product or review to remove directly. Empty state: "Nothing to review right now."### 3.5 Reports (`/admin/reports`)Range segmented control (7d, 30d, 90d, 365d). Key numbers (Total orders, Revenue), **Revenue by market** (horizontal SVG bars), **Most active Farmers** (table), **Orders over time** (SVG line or bars), Top products, New members. **Export CSV** secondary buttons (Orders, Revenue, Farmers) that download only on click (`fetch` with the token, then a Blob download; show progress and errors), and a small **Report history** list from `/admin/reports/history`.### 3.6 Settings (`/admin/settings`)`Tabs`: **Categories | Announcements | Messages | Platform**. Categories: list with drag-free reorder (up/down buttons with 44px targets), add/edit sheet (name, active), delete with the 409 "deactivate instead" handling. Announcements: list with status (Live, Scheduled, Expired), create/edit sheet (title, body, audience select, publish now or schedule, expiry), delete. Messages: contact messages inbox with "Mark handled". Platform: the three settings with validation and "Save changes".---## 4. Cross-cutting requirements- **Route protection**: `ProtectedRoute` by role; a wrong-role visit shows Unauthorized; `pending` Farmers can open the Farmer area. Sign out from the More sheet / account menu.- **States**: loading (skeletons matching layout), empty (illustration + one sentence + one button), error with retry, offline banner, 401 refresh, 403/404/409/422/429 messages mapped to warm text; field errors under fields.- **No placeholder data anywhere** in these areas; if something needs data the API lacks, **add it to the backend** with tests and docs.- **Layout quality**: `layoutCheck()` zero findings at 320, 360, 390, 768, 1024, 1440 on **every Farmer and Admin page and sheet, with real data loaded** (including long names and 100+ rows). Density: no more than one heading, one filter row and one primary per screen; whitespace as in the Customer app.- **Accessibility**: labels, focus management in sheets, `aria-live` for toasts and async results, table semantics, charts with text alternatives, colour never the only signal, reduced motion.- **Performance**: `React.lazy` code-splitting for the `/vendor` and `/admin` areas; charts and tables light; no duplicate requests; Lighthouse Accessibility 95+.- Copy: "Customer" and "Farmer" only, sentence case, warm and specific. No filler.- Update `docs/DESIGN_SYSTEM.md` with the Farmer/Admin patterns (navigation, tables, charts, forms in sheets, confirm steps).---## 5. Acceptance checklist- [ ] Every Farmer and Admin route in the route table renders a complete page or sheet using **real API data only**- [ ] A Farmer can: set up their stall and map pin, add/edit/delete products (with image upload), toggle sold out, apply the weekly template, close dates, handle orders through every status, read and reply to reviews, view insights- [ ] An Admin can: see the overview, approve/reject/suspend/reinstate Farmers, activate/deactivate Customers, manage markets with map pins, moderate flagged listings and reviews, view reports and export CSV, manage categories, announcements, messages and platform settings- [ ] A **pending** Farmer sees the waiting card, can edit their profile, cannot add products; approving them in the Admin area makes their products appear in the Customer app immediately (verified in a browser walkthrough with two windows)- [ ] Suspending a Farmer removes their products from the Customer app and signs them out on next refresh- [ ] Sheets, bottom nav (Farmer), sidebar (desktop), tables stacking, charts, forms and confirm steps all work on phone, tablet and desktop- [ ] All states (loading, empty, error, offline, 401, 403, 404, 409, 422, 429) exist and were exercised deliberately- [ ] `layoutCheck()` clean; no console errors or warnings; one filled beet button per screen/sheet/step- [ ] Any backend additions have tests and docs; the backend suite is still green---# MarketLink: Testing Gate (paste at the END of every stage prompt)**This stage is NOT finished until everything below is proven by running it.** Do not say "should work". Show evidence, or say "not verified".## B. For front-end integration stages (6, 7, 8)1. **Run backend and front end together** and walk through **every page and every action** in the browser (phone size and desktop size). For each page record: which API calls it makes (open the Network tab), the status codes, and that the UI shows real data.2. **Placeholder proof**: run `grep -rn "placeholders" src` (and any old hard-coded arrays) and show that **no page or component imports placeholder data**. Delete `placeholders.js` in Stage 8 once nothing uses it.3. **All states work with real data**: loading (skeletons), empty, error (server down, 401 expired token with automatic refresh, 403, 404, 422 field errors shown under fields, 429), and success. Try each on purpose (stop the server, use a wrong password, sign in as a suspended Farmer).4. **Auth flows**: sign in, refresh on page reload, sign out, expired access token refreshes silently, blocked accounts show a clear message, role redirects work, wrong-role access shows the Unauthorized page.5. **No console errors or warnings**, no failed network requests except the ones you triggered on purpose.6. **Performance**: no request waterfalls that could be one call, no duplicate calls on mount (React StrictMode double calls handled), lists paginated or cursor-loaded, images/illustrations unchanged in weight. Check Lighthouse on Home.7. Re-run the earlier front-end quality checks (`layoutCheck()` if present: zero findings at 360, 390, 768, 1024, 1440).8. **Report table**: page or sheet, API calls used, states tested (loading / empty / error / success), result.## A. For backend stages (2 to 5, and any backend additions in 8)1. **Write automated tests first-class, not as an afterthought.** For every new endpoint cover: the happy path, every validation error (422), missing or invalid token (401), wrong role (403), not found (404), conflicts (409), edge cases (empty lists, limits, boundary values), and ownership rules (a Farmer can never touch another Farmer's data; a Customer never sees another Customer's orders).2. **Run the full suite from a clean database** (`npm run seed` then `npm test`). Paste the real output (pass/fail counts, duration). **All earlier stages' tests must still pass** (regression). Fix every failure. Never delete or weaken a test to make it pass.3. **Manually call every new endpoint** with `curl` (or extend `scripts/smoke.sh`) and record the status code and a trimmed response for each. Test with each seeded role.4. **Performance proof**: run `explain('executionStats')` on every new query and confirm **no `COLLSCAN`**, `totalDocsExamined` close to `nReturned`, and projections in place. Measure response times on the seeded database and confirm the budget (reads under 50 ms, feeds and search under 80 ms, writes under 100 ms, excluding bcrypt). Log and fix anything slower.5. **Concurrency and integrity proof** where relevant (stock, order numbers, unique constraints): run parallel requests in a test and show the results are correct (no overselling, no duplicate order numbers).6. **Security check on the new routes**: injection attempts, oversized input, role bypass attempts, IDOR attempts (changing an id in the URL to someone else's), and rate limits where applicable.7. **Update `docs/API.md` and `docs/DATABASE.md`** so they match reality (every route documented with an example).8. **Report table**: endpoint, method, auth, tests written, tested manually (yes/no), result. Any "no" must be explained and fixed before finishing.9. Confirm the front end still starts and no front-end file was broken.## Rules for the report at the end of every stage- Start with **"Stage N status: PASS or FAIL"** and justify it.- Include the **real command output** or screenshots of results.- List **what you did not verify** and **what you would test next**.- If any check fails, **fix it and re-run**; do not hand back a failing stage.